[project]
name = "reckoner"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["jsonschema[format]>=4,<5"]

[build-system]
requires = ["hatchling>=1.26,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/reckoner"]
